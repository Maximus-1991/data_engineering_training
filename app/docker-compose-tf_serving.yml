version: '3.3'
services:
    tensorflow-serving:
        image: 'tensorflow/serving'
        ports:
            - '8500:8500'
            - '8501:8501'
        volumes:
            - ./model_binaries:/models/image_classification
        environment:
            - MODEL_NAME=image_classification
